# Code Reviewer Memory

- [Nav migration pattern](nav_migration_pattern.md) — openTripCard in AppContent does not clear selectedConversation; overlays above the navigator can hide pushed cards.
- [Dead imports pattern](dead_imports_pattern.md) — TripsScreen tends to accumulate dead Reanimated imports (SlideInRight, SlideOutRight) when overlay code is deleted; styles also linger.
- [AppContent effect dep risk](appcontent_effect_deps.md) — Effects in AppContent that call openTripCard/requestTab must include them in deps; they are stable useCallbacks with [] deps today but this is fragile if they grow deps.
- [useCallback deps miss new closure vars](rendercallback_deps_pattern.md) — recurring: TripDeck renderItem missed userId, CreateTripFlowA validateStep misses stripeReady; async-set state is the worst case.
- [REQUIREMENT_ORDER drives the wizard](requirement_catalog_drives_wizard.md) — new kinds auto-appear as toggles; use `isPayKind`; a managed trip may have no deposit row.
- [Notification foreground gate pattern](notification_foreground_gate_pattern.md) — module-level "screen open" flags gate push banners; check single-instance route, live getter (not captured), background short-circuit.
- [Channel consolidation drops guards](channel_consolidation_drops_guards.md) — merging N subscriptions into 1 shared hub silently drops per-call-site defensive guards (e.g. `row?.id` check); diff old vs new line-by-line, don't trust "handlers stay identical".
- [In-app banner overlay review (2026-07-04)](in_app_banner_overlay_review.md) — ready-to-commit verdict + open minors (as-any casts, missing group-title fallback, is_system not excluded from banners) for bellBannerSource/notificationsRealtimeHub/InAppBannerHost/MessagingProvider banner pass.
- [Banner pass DM guard gap](banner_pass_dm_guard_gap.md) — banner's onPress can pass otherUserId='' if conv.other_user missing; ConversationsScreen's notif-tap path guards this but banner-tap doesn't.
- [Project has no i18n/netlify-functions](project_no_i18n_netlify.md) — Swellyo has no src/locales or netlify/functions; the generic reviewer persona's dual-sync/i18n hard rules don't apply here, don't false-flag them.
- [Supabase grant gotchas](supabase_grant_gotchas.md) — `revoke from public, anon` leaves authenticated holding ALL; new columns on users/participants are client-writable via column-blind RLS.
- [Payments migration + edge-fn reviews (2026-08-03)](payments_migration_review.md) — running record of the Stripe DB/edge-fn defects, incl. the whole-branch review's 3 cross-task Criticals.
- [is_trip_host vs host_id](is_trip_host_vs_host_id_split.md) — is_trip_host = any promoted admin; host_id = the one who gets paid — but host_id is itself PATCH-able by any admin.
- [functions.invoke swallows the error body](functions_invoke_swallows_error_body.md) — non-2xx ⇒ `data` is null and `error.message` is generic; every `data?.error` fallback in src/ is dead code.
- [Postgres GREATEST ignores NULL](pg_greatest_ignores_null.md) — `greatest(x, 0)` turns an unknown amount into 0, which downstream reads as "nothing owed / approved".
- [Edge fn secret fail-open](edge_fn_secret_fail_open.md) — `Deno.env.get('X')!` is not a runtime check; an unset HMAC secret encodes to an empty, guessable key.
- [BottomSheetShell is headless](bottom_sheet_shell_is_headless.md) — a new sheet must bring its own white surface + `insets.bottom` padding, or it floats on the scrim / hides under the Android nav bar.
- [TravelerPriceSheet review (Task 10)](travel_price_sheet_review.md) — nested-Modal placement verified correct; defects are the missing surface and a confirm prompt that fires on price DECREASES.
- [Money display rounding trap](money_display_rounding_trap.md) — formatPrice snaps ₪ to the nearest 100 and Stripe charges USD; fine for browsing, wrong on a row the user is about to pay.
- [Client/SQL amountDue mirror drift](client_sql_amount_due_mirror.md) — tripPaymentsService.amountDue and operator_traveler_amount_due() disagree on the deposit branch despite the "these mirror" header.
- [Native bottom-tabs migration review](native_bottom_tabs_migration_review.md) — native tab bar migration is clean (focus-gating intact); watch for dead TripsBottomNav control code, silent landing-tab changes, and app.json plugins being inert since native folders are committed and prebuild is skipped.
