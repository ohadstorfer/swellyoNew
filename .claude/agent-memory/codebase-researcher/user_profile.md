---
name: user-profile-ohad
description: Ohad is a dev on the ohad branch; prefers short responses, English only, autonomous problem solving, reviews/commits manually
metadata:
  type: user
---

Ohad Storfer (ohad.storfer@gmail.com) works on the `ohad` branch, git user "Ohad
Storfer". (Note: CLAUDE.md's "Eyal's preferences" section and this file were
originally written around Eyal, the other Swellyo dev on the `eyal` branch — Ohad
shares the same working-style preferences, confirmed by the main conversation's
own memory index.)

- Keep responses short. Brief summary after changes. Longer explanations only for large implementations.
- English only in code, comments, and conversation.
- Propose better patterns but ask before refactoring existing code.
- Be autonomous — only ask when input will prevent going in circles.
- When spotting an unrelated bug: finish current task first, flag at the end.
- Do NOT commit — reviews and commits manually.
- Does not want simulator/Maestro UI verification — verify via code + tsc, tests on-device himself.
