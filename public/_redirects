# Netlify SPA redirects
# Note: Static files are handled automatically by Netlify
# Only redirect non-existent routes to index.html for SPA routing
# The catch-all is handled in netlify.toml with force=false
