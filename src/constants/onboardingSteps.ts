/**
 * Onboarding / auth flow step constants.
 * Used for navigation and routing so "welcome" and other steps are not magic numbers.
 */

/** Step value for the main Welcome screen (pre-login, pre-onboarding). */
export const STEP_WELCOME = -1;

/** Step 0: Onboarding welcome/explanation screen (after login, before step 1). */
export const STEP_ONBOARDING_WELCOME = 0;
