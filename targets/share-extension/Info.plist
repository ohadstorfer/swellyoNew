<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
  <dict>
    <key>NSExtension</key>
    <dict>
      <key>NSExtensionPointIdentifier</key>
      <string>com.apple.share-services</string>
      <key>NSExtensionPrincipalClass</key>
      <string>$(PRODUCT_MODULE_NAME).ShareViewController</string>
      <key>NSExtensionAttributes</key>
      <dict>
        <!--
          Predicate form, NOT the dictionary form. The dictionary form cannot
          match a shared contact:

            `public.vcard` conforms to BOTH `public.data` and `public.text`
            (supertypes: public.contact, public.content, public.data,
             public.item, public.text — verified with UTType.vCard.supertypes).

          `NSExtensionActivationSupportsFileWithMaxCount` only claims items that
          are files and NOT text/image/movie/url, and
          `NSExtensionActivationSupportsText` means *plain* text
          (public.plain-text). A vCard falls between the two, so Swellyo never
          appeared in the Contacts share sheet.

          Naming public.vcard explicitly is the only rule that matches it.
        -->
        <key>NSExtensionActivationRule</key>
        <string>SUBQUERY (
            extensionItems, $extensionItem,
            SUBQUERY (
                $extensionItem.attachments, $attachment,
                ANY $attachment.registeredTypeIdentifiers UTI-CONFORMS-TO "public.vcard"
                || ANY $attachment.registeredTypeIdentifiers UTI-CONFORMS-TO "public.image"
                || ANY $attachment.registeredTypeIdentifiers UTI-CONFORMS-TO "public.movie"
                || ANY $attachment.registeredTypeIdentifiers UTI-CONFORMS-TO "public.url"
                || ANY $attachment.registeredTypeIdentifiers UTI-CONFORMS-TO "public.plain-text"
            ).@count &gt; 0
        ).@count &gt; 0</string>
      </dict>
    </dict>
    <!-- Must track the app target. PRE_BUILD_CHECKLIST tracks 5 version spots;
         this target and notify-service make 6 and 7. -->
    <key>CFBundleVersion</key>
    <string>41</string>
    <key>CFBundleShortVersionString</key>
    <string>1.3.2</string>
  </dict>
</plist>
